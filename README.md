# Expense Application

## Курсова робота  
### «Розробка системи ревізування витрат організацій»

**Студенти:**  
Смагула Олександр Андрійович  
Біжик Юрій Володимирович  

**Група:** ФЕІ-27  

**Email:**  
sasha.smagula@gmail.com  
qwantiix01@gmail.com  

**Репозиторій:**  
https://github.com/SenRey007/ExpenseApplication

---

## Опис проєкту

Expense Application — це веб-додаток для управління витратами організації. Система дозволяє співробітникам створювати запити на відшкодування витрат, а адміністраторам — переглядати, схвалювати або відхиляти ці запити. Додаток включає автоматичну обробку платежів через фонові завдання та систему звітності.

### Основні можливості:
- 📝 Створення та управління запитами на витрати
- ✅ Схвалення/відхилення запитів адміністратором
- 💳 Симуляція обробки платежів через фонові завдання (Hangfire)
- 📊 Генерація звітів (щотижневі, щомісячні)
- 👥 Управління користувачами та ролями (Admin, Personnel)
- 🔐 JWT автентифікація

---

## Технології

- **Backend:** .NET 8 (ASP.NET Core Web API)
- **База даних:** PostgreSQL (через Docker)
- **ORM:** Entity Framework Core
- **Автентифікація:** JWT (JSON Web Tokens)
- **Фонові завдання:** Hangfire
- **Контейнеризація:** Docker, Docker Compose
- **Міграції:** EF Core Migrations

---

## Передумови

Перед запуском проєкту переконайтеся, що у вас встановлено:

- [.NET SDK 8](https://dotnet.microsoft.com/download)
- [Docker](https://www.docker.com/get-started) та Docker Compose
- [Git](https://git-scm.com/downloads)

---

## Встановлення та запуск

### 1. Клонування репозиторію
```bash
git clone https://github.com/SenRey007/ExpenseApplication.git
cd ExpenseApplication
```

### 2. Запуск Docker контейнерів

Виконайте команду для запуску PostgreSQL та інших сервісів:
```bash
docker-compose down -v --remove-orphans && docker-compose up
```

### 3. Застосування міграцій бази даних

Перейдіть до папки проєкту та виконайте міграції:
```bash
cd ExpenseApplication
dotnet ef migrations add InitialCreate --project Infrastructure --startup-project Api
dotnet ef database update --project Infrastructure --startup-project Api
```

**Якщо міграції вже існують**, можна скинути базу даних:
```bash
dotnet ef database update --project Infrastructure --startup-project Api
```

### 4. Доступ до додатку

Додаток буде доступний за адресою: **http://localhost:5245**

---

## Діаграма бази даних (ERD)

![ERD Diagram](.github/assets/erd.png)

**Опис полів:**
- **CreatedBy:** Адмін, який створив користувача
- **Status:** Тільки адмін може схвалити або відхилити запит на витрати
- **Description:** Опис витрат від користувача
- **PaymentDescription:** Опис платежу від адміна або код помилки з фонового завдання

---

## API Endpoints

### 🔐 Автентифікація

#### Генерація токену
| Метод | Шлях |
|-------|------|
| POST | `/api/Token` |

**Опис:** Генерує JWT токен для автентифікації.  
**Ролі:** Admin, Personnel

---

### 💰 Управління витратами (Expense)

#### Створити запит на витрати
| Метод | Шлях |
|-------|------|
| POST | `/api/Expense/` |

**Доступ:** `[Admin, Personnel]`

#### Отримати всі витрати
| Метод | Шлях |
|-------|------|
| GET | `/api/Expense/` |

**Доступ:** `[Admin]`

#### Оновити витрати
| Метод | Шлях |
|-------|------|
| PUT | `/api/Expense/{expenseRequestId}` |

**Доступ:** `[Admin]`  
**Примітка:** Не запускає фонове завдання, використовується для ручного оновлення.

#### Видалити витрати
| Метод | Шлях |
|-------|------|
| DELETE | `/api/Expense/{expenseRequestId}` |

**Доступ:** `[Admin]`

#### Отримати витрати за ID
| Метод | Шлях |
|-------|------|
| GET | `/api/Expense/{expenseRequestId}` |

**Доступ:** `[Admin]`

#### Схвалити витрати
| Метод | Шлях |
|-------|------|
| PATCH | `/api/Expense/Approve/{expenseRequestId}` |

**Доступ:** `[Admin]`  
**Опис:** Схвалює запит та запускає фонове завдання для обробки платежу.

#### Відхилити витрати
| Метод | Шлях |
|-------|------|
| PATCH | `/api/Expense/Reject/{expenseRequestId}` |

**Доступ:** `[Admin]`  
**Опис:** Відхиляє запит без запуску фонового завдання.

#### Отримати витрати користувача
| Метод | Шлях |
|-------|------|
| GET | `/api/Expense/ByUser` |

**Доступ:** `[Admin, Personnel]`  
**Опис:** Персонал бачить тільки свої витрати. Адмін може фільтрувати за статусом і датою.

---

### 📁 Категорії витрат (ExpenseCategory)

#### Створити категорію
| Метод | Шлях |
|-------|------|
| POST | `/api/ExpenseCategory` |

**Доступ:** `[Admin]`

#### Отримати всі категорії
| Метод | Шлях |
|-------|------|
| GET | `/api/ExpenseCategory` |

**Доступ:** `[Admin]`

#### Оновити категорію
| Метод | Шлях |
|-------|------|
| PUT | `/api/ExpenseCategory/{expenseCategoryId}` |

**Доступ:** `[Admin]`

#### Видалити категорію
| Метод | Шлях |
|-------|------|
| DELETE | `/api/ExpenseCategory/{expenseCategoryId}` |

**Доступ:** `[Admin]`  
**Примітка:** Категорію не можна видалити, якщо є активні витрати.

---

### 💳 Симуляція платежів (PaymentSimulator)

#### Обробити платіж
| Метод | Шлях |
|-------|------|
| POST | `/api/PaymentSimulator/ProcessPayment` |

**Опис:** Використовує Hangfire для симуляції обробки платежу у фоновому режимі.

---

### 📊 Звіти (Report)

#### Звіт по схвалених платежах
| Метод | Шлях |
|-------|------|
| POST | `/api/Report/ApprovedPaymentFrequencyReport` |

**Доступ:** `[Admin]`  
**Приклад відповіді:**
```json
{
  "type": "monthly",
  "startDate": "01/01/2025 00:00:00",
  "endDate": "31/01/2025 23:59:59",
  "approvedCount": 16,
  "approvedSum": 14400,
  "averageApprovedAmount": 900
}
```

#### Звіт по відхилених платежах
| Метод | Шлях |
|-------|------|
| POST | `/api/Report/RejectedPaymentFrequencyReport` |

**Доступ:** `[Admin]`

#### Звіт по витратах персоналу
| Метод | Шлях |
|-------|------|
| POST | `/api/Report/PersonnelExpenseFrequencyReport` |

**Доступ:** `[Admin]`

#### Підсумковий звіт персоналу
| Метод | Шлях |
|-------|------|
| POST | `/api/Report/PersonnelSummaryReport` |

**Доступ:** `[Admin, Personnel]`  
**Опис:** Персонал бачить тільки свої витрати.

---

### 👥 Управління користувачами (User)

#### Створити користувача
| Метод | Шлях |
|-------|------|
| POST | `/api/User` |

**Доступ:** `[Admin]`

#### Отримати всіх користувачів
| Метод | Шлях |
|-------|------|
| GET | `/api/User` |

**Доступ:** `[Admin]`

#### Оновити користувача
| Метод | Шлях |
|-------|------|
| PUT | `/api/User/{UserId}` |

**Доступ:** `[Admin]`

#### Видалити користувача
| Метод | Шлях |
|-------|------|
| DELETE | `/api/User/{UserId}` |

**Доступ:** `[Admin]`  
**Примітка:** Користувача не можна видалити, якщо є активні витрати.

#### Активувати користувача
| Метод | Шлях |
|-------|------|
| PATCH | `/api/User/ActivateUser` |

**Доступ:** `[Admin]`  
**Опис:** Тільки адмін може активувати заблокованого користувача.

#### Деактивувати користувача
| Метод | Шлях |
|-------|------|
| PATCH | `/api/User/DeactivateUser` |

**Доступ:** `[Admin]`  
**Опис:** Деактивовані користувачі не можуть входити в систему.

---

## Особливості реалізації

### Автентифікація та авторизація
- Використовується JWT токени
- Ролі: **Admin** (повний доступ), **Personnel** (обмежений доступ)
- Автоматичне блокування після 3 невдалих спроб входу

### Фонові завдання (Hangfire)
- Автоматична обробка платежів після схвалення
- Симуляція затримки обробки платежу
- Оновлення статусу платежу в базі даних

### Безпека
- Хешування паролів
- JWT токени з обмеженим терміном дії
- Авторизація на рівні endpoint'ів

---

## Структура проєкту
```
ExpenseApplication/
├── Api/                             
├── Business/                 
├── Infrastructure/
├── Schemes/      
├── docker-compose.yml    
└── README.md              
```

---

## Контакти

**Студенти:**  
Смагула Олександр Андрійович  
Біжик Юрій Володимирович   

**Email:**  
sasha.smagula@gmail.com  
qwantiix01@gmail.com  

**GitHub:** 
https://github.com/SenRey007/ExpenseApplication

---

## Ліцензія

Цей проєкт створено в рамках курсової роботи.